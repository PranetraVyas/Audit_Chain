# Minimal requirements without web3 (for development without C++ build tools)
# Use this if you cannot install web3 due to C++ build tools requirement

fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Note: web3 is optional - the system will use a fallback implementation
# To install web3 later: pip install web3 (requires C++ build tools on Windows)


